import React from "react";
import Navbar from "../navbar/navbar";

const Header = (props) => {
  return (
    <div>
      <Navbar/>
      <h2>welcome to JSX Checkpoint</h2>
    </div>
  );
};

export default Header;
